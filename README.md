# Calendar PWA

中国农历日历PWA应用 - 支持农历、节日、节气查询

## 功能特性

- 📅 公历和农历日期显示
- 🎉 中国传统节日和法定假日
- 🌙 二十四节气
- 📱 PWA功能(可安装、离线使用)
- 🎨 现代化的响应式UI设计

## 技术栈

- **纯HTML/CSS/JavaScript** - 无构建工具依赖
- **TypeScript** - 仅用于类型检查和编译
- **lunar-javascript** - 农历计算库
- **Service Worker** - PWA离线支持

## 安装依赖

```bash
npm install
```

## 开发

1. 编译TypeScript代码:
```bash
npm run build
```

2. 监听文件变化自动编译:
```bash
npm run watch
```

3. 启动本地服务器:
```bash
npm run serve
```

然后在浏览器中打开 http://localhost:8080

## 项目结构

```
calendar-pwa/
├── src/                    # TypeScript源代码
│   ├── main.ts            # 应用入口
│   ├── calendar.ts        # 日历核心逻辑
│   ├── lunar.ts           # 农历计算
│   └── holidays.ts        # 节假日数据
├── public/                # 静态资源和编译输出
│   ├── index.html         # 主HTML文件
│   ├── manifest.json      # PWA配置
│   ├── sw.js             # Service Worker
│   ├── css/
│   │   └── styles.css    # 样式文件
│   ├── js/               # 编译后的JavaScript
│   └── *.png             # 应用图标
├── package.json
└── tsconfig.json
```

## PWA安装

在支持PWA的浏览器中访问应用后,可以通过浏览器菜单选择"安装应用"或"添加到主屏幕"。

## License

MIT
